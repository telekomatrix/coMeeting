<% if @participation.is_admin %>
	<%= text_area :minutes, nil, :id => 'minutes', :value => @minutes, :class => "minutes", :onclick => "_idleNow = true;" %>
<% else %>
	<%= text_area :minutes, nil, :id => 'minutes', :value => @minutes, :class => "minutes", :disabled => true %>
<% end %>