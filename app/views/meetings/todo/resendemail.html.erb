<%= link_to image_tag("/assets/icons/email.png") + " Send Email", reinvite_path(:link => @participation.link, :id => @participation.user_id), :remote => true %> <%# last parameter needs to be changed %>


<%= link_to image_tag("/assets/icons/email.png") + " Send Email", authenticate_path(:participation => @participation.link), :remote => true %>